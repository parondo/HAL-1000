visual_token = {
    'rgb': torch.randn(1, 3, 224, 224),  # [batch, channels, height, width]
    'depth': torch.randn(1, 1, 224, 224),  # Depth information
    'metadata': {
        'resolution': (224, 224),
        'color_space': 'RGB',
        'temporal_context': 0.0
    }
}
