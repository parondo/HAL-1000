math_token = {
    'expression': "∀x∈ℝ: ∃y∈ℂ: y² = x",
    'structure': torch.randn(1, 64, 512),  # Symbolic structure
    'numerical': torch.randn(1, 32, 256),  # Numerical data
    'metadata': {
        'domain': 'complex_analysis',
        'abstraction_level': 0.9,
        'proof_context': True
    }
}
